DATA SEGMENT
    FIB_ARRAY DB 20 DUP(0)
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA

START:
    MOV AX, DATA
    MOV DS, AX

    MOV AL, 1
    MOV [FIB_ARRAY], AL

    MOV AL, 1
    MOV [FIB_ARRAY + 1], AL

    MOV CX, 18
    MOV SI, 2

FIB_LOOP:
    MOV AL, [FIB_ARRAY + SI - 2]
    ADD AL, [FIB_ARRAY + SI - 1]
    MOV [FIB_ARRAY + SI], AL

    INC SI
    LOOP FIB_LOOP

    MOV AH, 4Ch
    INT 21h

CODE ENDS
END START